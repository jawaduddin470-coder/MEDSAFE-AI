# Render Build Troubleshooting Marker - v1.0.1
services:
  - type: web
    name: medsuree-app
    runtime: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
